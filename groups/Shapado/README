# Read-only repository setup
cd %GROUPROOT%
git git clone git://gitorious.org/shapado/shapado.git shapado

# Configuration
Add the following line to LocalSettings.php to include all Shapado groups:
require("$IP/extensions/Translate/groups/Shapado/Shapado.php");

Add one or more of the following lines to LocalSettings.php to include
individual Shapado groups:
$wgTranslateGroupFiles[] = "$IP/extensions/Translate/groups/Shapado/ads.yml";

# Commits
To do.
